import java.awt.*;
import java.applet.*;

public class slip15_b extends Applet {
    void Paint(Graphics g) {
        g.setColor(Color.yellow);
        g.fillOval(200, 200, 700, 700);
        g.setColor(Color.white);
        g.fillOval(250, 250, 100, 100);
    }
}
